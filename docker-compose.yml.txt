version: '3.8'

services:
  database:
    image: mongo:latest
    container_name: restaurant_db
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  backend:
    build: ./backend
    container_name: restaurant_backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb://database:27017/smart-restaurant
      - JWT_SECRET=your_super_secret_jwt_key_12345
    depends_on:
      - database

volumes:
  mongo_data:
